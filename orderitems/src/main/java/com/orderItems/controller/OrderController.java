package com.orderItems.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;


@RestController
public class OrderController {
	/*
	 * @Autowired private RestaurantMenuService restaurantmenuService;
	 * 
	 * @PostMapping("/restaurantsmenu") public RestaurantMenu
	 * createRestaurantMenu(@RequestBody RestaurantMenu restaurantmenu) { return
	 * restaurantmenuService.createRestaurantMenu(restaurantmenu); }
	 */

}
